# Timetablist app

タイムテーブル作成専用app

# 非機能仕様

- アプリ名: Timetablist
- iOS / Android / Web対応
- 技術スタック
  - フレームワーク: Flutter / Dart
  - アーキテクチャ: RiverpodベースのMVVM (Freezed使用)
- アカウント不要
  - データ保存はローカルストレージ＋テキストexport
- 英語 / 日本語対応
  - 言語切替はOS言語に従う
- 時刻・所要の表示形式
  - 特に個別の指定がなければ 24時間制・ゼロパディングなし
- ArchemistによるVRT作成

# 基本フロー
- どんな枠タイプがあるか決める（演奏・転換）など
- タイムラインを作る
- 枠タイプを配置して実際の枠を作る
  - タイムラインがない場合は、配置しようとした時に作成を促す
- 枠に演者を割り当てる
  - 出演枠属性（後述）を付与した枠には演者を割り当てられる

# データオブジェクト

### ドキュメント

ひとまとまりのデータ。エクスポート／インポートはこの単位で行う

- ドキュメント名
- タイムラインのリスト

### タイムライン
- タイムラインID
- タイムライン名
- 枠リスト

※ リストは順序つきリスト
※ 開始時刻・終了時刻はタイムライン側では保持しない（リスト先頭の枠の開始時刻、リスト末尾の枠の終了時刻から都度計算）

### 枠タイプ (SlotCategory)

枠の種類・属性を規定する

- 枠タイプID
- 名前
- 時間長
- 出演枠属性の有無

- [TBD] 色など見た目の区別をするか

※ 名前が同一の枠タイプが存在する場合、表示名を `名前(時間長)` とする。表示名は保持プロパティとはせず、都度判定・計算して生成する

### 枠 (Slot)

実際にタイムライン上に配置される枠

- 枠ID
- 枠タイプ
- 開始時間
- 演者

※ 終了時間は保持プロパティとせず、「開始 + 枠タイプの時間長」で常に算出する

### 演者（Participant）

出演枠に配置される演者の情報。要求事項は配置可否の制御には用いず、警告の表示のためにだけ用いる

- 演者ID
- 名前
- 要求(optional)
   - 最低時間
   - 最長時間
   - 〜までに終了したい
   - 〜以降に開始したい
   - 希望出演順(range)
     - 出演順 range は、出演枠だけの何番目かを表す

※ 名前が重複する場合、表示名としては `名前(演者ID)` とする。表示名は保持せず、都度算出する


# 画面構成
- 画面は左上・左下・中・右の4ペイン構成とする
  - 左上：枠タイプペイン
     - 「枠タイプ」のリストと枠タイプ作成ボタン
     - ここに並んでいる「枠タイプ」をタイムラインにドラッグすることで実際の「枠」が作成される
     - 縦スクロール可能
  - 左下：演者ペイン
     - 「演者」のリストと演者作成ボタン
     - ここに並んでいる「演者」を、タイムラインに配置された「枠」にドラッグすることで、枠に演者が割り当てられる
     - 縦スクロール可能
  - 中：タイムラインペイン
     - 「タイムライン」のリストとタイムライン作成ボタン
       - 複数タイムラインがある場合、横に並べる
     - 縦軸に時間目盛りがある
         - 0時〜24時。
         - 目盛り間隔は絶対でなく、タイムラインオブジェクト・枠オブジェクトの配置によって伸縮する
     - 縦横スクロール可能
  - 右：プロパティペイン
     - 選択中のオブジェクトのプロパティを表示。プロパティ変更もできる
     - 縦スクロール可能

# 画面コンポーネント

- タイムテーブル構成コンポーネント
  - タイムラインコンポーネント
    - 表示項目
      - タイムライン名
        - 小さいフォントで表示
      - 内包する枠コンポーネント
        - 内包にあたっては、パディングなしとすることで枠をドラッグ等で移動できない印象とする
        - 隣接する枠コンポーネント間のギャップも不要
    - 幅は固定
    - 高さは内包コンポーネントにより可変
  - 枠コンポーネント
    - 表示項目
      - 枠名
        - 小さいフォントで表示
    - 演者コンポーネント
      - 出演枠属性が付与されている場合のみ
      - １つだけ内包可能
      - 内包にあたっては、若干のパディングをつけ、ドラッグ等で移動できる印象とする
    - 角丸なしとすることで、上記のパディングなしと併せてドラッグ等で移動できない印象とする
    - 幅は親コンポーネントに従う
    - 高さは最低高さがある。演者コンポーネントがある場合、内包できるよう必要に応じて拡張
  - 演者コンポーネント
    - 表示項目
      - 演者名
        - 見やすくはっきりとしたフォントで表示
      - 要求不一致警告
        - 演者の要求と合わない項目がある場合に表示
        - 警告色の小さいフォントで表示
        - 文言は固定（プロパティペインに詳しく表示）
    - 角丸をつけ、上記のパディングと併せてドラッグ等で移動可能な印象とする
    - 幅は親コンポーネントに従う
    - 高さは演者名のテキスト量に従う

- [TBD] 使用中のカテゴリを消したとき、配置済み枠はどうなるか


# 操作時挙動

### 枠タイプペインからタイムラインペインに枠オブジェクトをドラッグ＆ドロップしたとき
- ドラッグ中は挿入予定位置を示すバーを表示
- ドラッグ時の時間解像度は10分
  - ただし、プロパティペインからは1分単位で指定が可能
- ドラッグ先がタイムラインオブジェクトの場合
  - ドロップするとその位置に枠オブジェクトを挿入
- ドラッグ先がタイムラインオブジェクトでない（外）の場合
  - 当該の枠のみを含むタイムラインを作成するか、テンプレートを作成するかを聞くダイアログを表示
    - 当該の枠のみを含むタイムラインを作成する場合、
      - ドラッグ位置を基準に開始時間を決めてタイムラインを作成、そこに枠を配置する
    - テンプレートから作成する場合、
      - 下記「シーケンス作成」と同じフローに入る

### 演者ペインからタイムラインペインに演者オブジェクトをドラッグ＆ドロップしたとき
- ドラッグ開始で演者ペインから演者オブジェクトが消える
  - したがって、１つの演者を複数の枠に配置はできない
    - 複数の枠に割り当てたい場合は別演者として演者を作成する
- ドラッグ先が枠オブジェクトの場合
  - 枠に演者が設定されていなければ、ドラッグ中の演者を設定
  - 枠に演者がすでに設定されていれば、すでに設定されている旨をダイアログ表示、入れ替えるを選んだら入れ替える
  - 入れ替えない場合、演者ペインに演者オブジェクトを戻す
- ドラッグ先が枠オブジェクト以外の場合
  - 枠タイプが未作成の場合
    - 出演枠属性を付与した枠タイプを作成し、それを枠として配置＋そこに演者を割り当てる
      - 演者側に希望時間長が設定されていればそれを枠の長さに採用
      - 希望時間長が未設定の場合、時間長を入力させるダイアログを表示、その入力に従って枠を作成する
  - 作成済みの出演者属性付き枠タイプが１つだけ存在する場合
    - その枠タイプで枠を配置＋そこに演者を割り当てる
      - 演奏者側の希望項目と合わなくても一旦OK
  - 作成済みの出演者属性付き枠タイプが２つ以上存在する場合
    - 枠タイプ選択ダイアログを出す
      - 演奏者希望項目とマッチしない項目がある枠には、その旨表示する
        - 小さい文字・警告色で、警告文言を枠タイプ名と併記する

## タイムラインペインの中の枠オブジェクトの中の演者オブジェクトをドラッグしたとき

- ドラッグ対象の演者オブジェクトは枠オブジェクトから消える
- ドラッグ中の演者オブジェクトの動作は、演者ペインからドラッグしたのと同じ
- ドロップできなかった場合は、元の枠に戻る

※枠オブジェクトはタイムラインペインの中に入れたらドラッグできない→自由にドラッグできなそうな見た目にしたい
- [TBD] 配置後の枠の順序変更・開始時間変更の操作（プロパティ / 上下ボタン など）
- [TBD] 演者の割り当て解除（プロパティからの解除など、ドラッグ以外）


# シーケンス作成

タイムラインで作成ボタンを押すと、以下の設定を行う画面を表示する

## タイムライン設定項目

`[ ]` は、チェックを入れた場合に設定可能となる項目

- シーケンス開始時間
- [ ] 準備あり
    - 枠タイプ
      - すでに作成済みの非出演枠属性の枠タイプがある場合
        - プルダウンで 既存から選択 or 新規作成 を選択
          - 新規の場合、枠タイプ名と時間長を入力するフィールドを表示
          - 既存の場合、枠タイプ名と時間長を編集不可で表示
      - 非出演枠属性の枠タイプがない場合
        - 枠タイプ名と時間長を入力するフィールドを表示。名前は初期値として「準備」という名前を入れておく
- 出演枠
    - 開始時刻（※表示のみ、シーケンス開始時間と準備時間から算出）
    - 枠数
    - 枠タイプ
      - すでに作成済みの出演枠属性の枠タイプがある場合
        - プルダウンで 既存から選択 or 新規作成 を選択
          - 新規の場合、枠タイプ名と時間長を入力するフィールドを表示
          - 既存の場合、枠タイプ名と時間長を編集不可で表示
      - 非出演枠属性の枠タイプがない場合
        - 枠タイプ名と時間長を入力するフィールドを表示。名前は初期値として「出演枠」という名前を入れておく

- [ ] インターバルあり
    - 枠タイプ
      - すでに作成済みの非出演枠属性の枠タイプがある場合
        - プルダウンで 既存から選択 or 新規作成 を選択
          - 新規の場合、枠タイプ名と時間長を入力するフィールドを表示
          - 既存の場合、枠タイプ名と時間長を編集不可で表示
      - 非出演枠属性の枠タイプがない場合
        - 枠タイプ名と時間長を入力するフィールドを表示。名前は初期値として「転換」という名前を入れておく

- [ ] 撤収あり
    - 開始時刻（※表示のみ、ここまでのシーケンスから算出）
    - 枠タイプ
      - すでに作成済みの非出演枠属性の枠タイプがある場合
        - プルダウンで 既存から選択 or 新規作成 を選択
          - 新規の場合、枠タイプ名と時間長を入力するフィールドを表示
          - 既存の場合、枠タイプ名と時間長を編集不可で表示
      - 非出演枠属性の枠タイプがない場合
        - 枠タイプ名と時間長を入力するフィールドを表示。名前は初期値として「撤収」という名前を入れておく

- 終了時刻（※表示のみ、ここまでのシーケンスから算出）

以上を入力して確定すると、以下のようにタイムラインが作成される。※入力中も下記の計算は行い、随時時刻表示に反映する

- 準備がある場合、先頭に配置
- その後に、枠数で指定した数だけ 出演枠→インターバル→出演枠・・・ と並べる
  - 出演枠が N 個の場合、インターバルは (N-1) 個となる
  - インターバルがなければ、単に出演枠を並べる
- 最後に撤収を配置

スロット編集
→同じ種類のスロットも変更するか？[このスロットだけ／全部]
  → 「だけ」の場合、新しいスロットカテゴリ作成
- [TBD] この確認をどのUIから出すか


# 作成・削除・複製
- [TBD] 枠タイプ / 演者 / タイムライン / 枠 それぞれの作成ダイアログ項目
- [TBD] 削除確認の有無
- [TBD] タイムライン削除時に中の枠ごと消えるか
- [TBD] 複製の有無

# 選択とプロパティペイン
- [TBD] 何も選んでいないときの表示（空 / ドキュメント全体の設定）
- [TBD] タイムライン / 枠 / 演者 / 枠タイプ それぞれで編集できる項目

# タイムライン上の時間
- [TBD] ドロップ位置から開始時間をどう決めるか（目盛り吸着、前後の枠の直後、など）
- [TBD] 挿入時に後続枠の開始時間をずらすか
- [TBD] 目盛りの最小単位（1分 / 5分 / 可変）
- [TBD] 24時をまたぐイベントを扱うか

# 空状態
- [TBD] 枠タイプ・演者・タイムラインが0件のときの表示と、最初の誘導

# 保存・入出力
- [TBD] ローカル保存の単位とタイミング（自動保存 / 明示保存）
- [TBD] テキストexportの中身（時刻と演者名、枠タイプ名を含めるか、コピー用かファイルか）
- [TBD] import はしないか
- [TBD] 端末間の引き継ぎはしないか

# イベント・会場

- [TBD] 以下の「イベント / 会場 / 演目」は上の Timeline / Slot / Participant と同一モデルか別案か
- [TBD] 1アプリ＝1タイムテーブルか、イベント＋会場（複数タイムライン）か
- [TBD] 会場が複数ある場合、タイムラインは会場ごと1本か

# カラースキーム

- カラーは Material Design に基づいて決める
- 具体的な色指定は仕様としては定義しない（実装時に調整）


項目マスタ
https://docs.google.com/spreadsheets/d/1QHhVQ7EJZNfDQ2P-Jauc1SaKmCB44iP33G8bqFcoJ0I/edit?gid=0#gid=0
