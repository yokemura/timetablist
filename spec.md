# Timetablist app

タイムテーブル作成専用app

未決事項は `[TBD]` で検索する。

# 非機能仕様

- iOS / Android / Web対応
  - [TBD] 技術スタック（Flutter / React Native / PWA など）
- アカウント不要
  - データ保存はローカルストレージ＋テキストexport
- 英語 / 日本語対応
  - [TBD] 言語切替（OS言語に従う / アプリ内切替）
- [TBD] 時刻・所要の表示形式（`18:00` / `18時`、分表示など）

# 基本フロー
- どんな枠タイプがあるか決める（演奏・転換）など
- タイムラインを作る
- 枠タイプを配置して実際の枠を作る
  - タイムラインがない場合は、配置しようとした時に作成を促す
- 枠に演者を割り当てる
  - 出演枠属性を付与した枠には演者を割り当てられる
  - [TBD] 出演枠属性はどの SlotCategory に付くか（デフォルト「演目」のみか、ユーザーが付け外しできるか）

# 画面構成
- 画面は左上・左下・中・右の4ペイン構成
  - 左上：枠ペイン
     - 「枠」のリストと枠作成ボタン
     - [TBD] リストに出るのは SlotCategory（タイプ）か、未配置の Slot か
  - 左下：演者ペイン
     - 「演者」のリストと演者作成ボタン
  - 中：タイムラインペイン
     - 「タイムライン」のリストとタイムライン作成ボタン
     - 縦軸に時間目盛りがある
         - 0時〜24時。
         - 目盛り間隔は絶対でなく、タイムラインオブジェクト・枠オブジェクトの配置によって伸縮する
  - 右：プロパティペイン
     - 選択中のオブジェクトのプロパティを表示。プロパティ変更もできる

# 画面オブジェクト
- タイムテーブル構成オブジェクト群
  - 共通表示要素
     - そのオブジェクトの表示名
     - 内包するオブジェクトを入れ子で表示
  - オブジェクト一覧
     - タイムラインオブジェクト
         - 枠オブジェクトを複数配置可能
     - 枠オブジェクト
         - 出演枠属性が付与されている場合、演者を１つ配置可能
     - 演者オブジェクト

# 操作時挙動

### 枠ペインからタイムラインペインに枠オブジェクトをドラッグ＆ドロップしたとき
- ドラッグ中は挿入予定位置を示すバーを表示
- ドラッグ先がタイムラインオブジェクトの場合
  - ドロップするとその位置に枠オブジェクトを挿入
- ドラッグ先がタイムラインオブジェクトでない（外）の場合
  - 当該の枠のみを含むタイムラインを作成するか、テンプレートを作成するかを聞くダイアログを表示
    - 当該の枠のみを含むタイムラインを作成する場合、
      - [TBD] 作成後の開始時間の決め方
    - テンプレートから作成する場合、
      - [TBD] テンプレートの定義（プリセット一覧、保存方法）

### 演者ペインからタイムラインペインに演者オブジェクトをドラッグ＆ドロップしたとき
- ドラッグ先が枠オブジェクトの場合
  - 枠に演者が設定されていなければ、ドラッグ中の演者を設定
  - 枠に演者がすでに設定されていれば、すでに設定されている旨をダイアログ表示、入れ替えるを選んだら入れ替える
- ドラッグ先が枠オブジェクト以外の場合
  - 枠の作成を促すダイアログを表示

タイムラインペインの中の枠オブジェクトの中の演者オブジェクトをドラッグすると

- ドラッグ対象の演者オブジェクトは枠オブジェクトから消える
- ドラッグ中の演者オブジェクトの動作は、演者ペインからドラッグしたのと同じ
- [TBD] ドロップできなかったとき、元の枠に戻るか

※枠オブジェクトはタイムラインペインの中に入れたらドラッグできない→自由にドラッグできなそうな見た目にしたい
- [TBD] 配置後の枠の順序変更・開始時間変更の操作（プロパティ / 上下ボタン など）
- [TBD] 演者の割り当て解除（プロパティからの解除など、ドラッグ以外）

# データオブジェクト

### ドキュメント（保存単位）
- [TBD] アプリを開いたとき何が復元されるか（直近1件のみ / 複数イベント）
- [TBD] 名前・作成日時など最低限のメタデータ

### Timeline
- [TBD] 名前、開始時間（0時固定か、イベント開始時刻か）
- [TBD] 枠の並び順の持ち方（配列順 / 開始時間順）
- [TBD] 枠と枠の間に隙間を許すか、常に隙間なく詰めるか

### SlotCategory（枠タイプ）
- 名前
- 時間長
- [TBD] 出演枠属性の有無
- [TBD] 色など見た目の区別をするか
- [TBD] 使用中のカテゴリを消したとき、配置済み枠はどうなるか
- [TBD] 名前重複時の `名前(時間長)` は表示専用か、内部名も変えるか

※デフォルトSlotCategory
  準備／演目／転換／撤収
※名前がかぶった場合、表示名を `名前(時間長)` とする

### Slot
- SlotCategory
- 開始時間
- [TBD] 終了時間は「開始 + Categoryの時間長」で常に算出するか
- [TBD] 同一 Participant を複数枠に置けるか

### Participant
- 名前
- 要求(optional)
   - 最低時間
   - 最長時間
   - 〜までに終了したい
   - 〜以降に開始したい
   - 希望出演順(range)
- [TBD] 要求は保存するだけか、配置時に警告するか（違反でも置けるか）
- [TBD] 希望出演順の range の意味（全体の何番目か、出演枠だけの何番目か）

# シーケンス作成

タイムラインで作成ボタンを押すと以下の選択肢を表示

- 空のタイムラインを作成
- 基本的な枠も同時に作成する

## 「基本的な枠も同時に作成」の設定項目

- シーケンス開始時間
- [ ] 準備あり
    - SlotCategory
      - 既存から選択 or 新規作成
         - 新規の場合、開始時間
- 出演枠
    - 開始時間
    - 枠数
    - SlotCategory
      - 既存から選択 or 新規作成
         - 新規の場合、持ち時間

- [ ] インターバルあり
    - SlotCategory
      - 既存or新規作成
      - 新規の場合、転換時間

- [ ] 撤収あり
    - SlotCategory
      - 既存から選択 or 新規作成
      - 新規の場合、終了時間


スロット編集
→同じ種類のスロットも変更するか？[このスロットだけ／全部]
  → 「だけ」の場合、新しいスロットカテゴリ作成
- [TBD] この確認をどのUIから出すか


# 作成・削除・複製
- [TBD] 枠タイプ / 演者 / タイムライン / 枠 それぞれの作成ダイアログ項目
- [TBD] 削除確認の有無
- [TBD] タイムライン削除時に中の枠ごと消えるか
- [TBD] 複製の有無

# 選択とプロパティペイン
- [TBD] 何も選んでいないときの表示（空 / ドキュメント全体の設定）
- [TBD] タイムライン / 枠 / 演者 / 枠タイプ それぞれで編集できる項目

# タイムライン上の時間
- [TBD] ドロップ位置から開始時間をどう決めるか（目盛り吸着、前後の枠の直後、など）
- [TBD] 挿入時に後続枠の開始時間をずらすか
- [TBD] 目盛りの最小単位（1分 / 5分 / 可変）
- [TBD] 24時をまたぐイベントを扱うか

# 空状態
- [TBD] 枠タイプ・演者・タイムラインが0件のときの表示と、最初の誘導

# 保存・入出力
- [TBD] ローカル保存の単位とタイミング（自動保存 / 明示保存）
- [TBD] テキストexportの中身（時刻と演者名、枠タイプ名を含めるか、コピー用かファイルか）
- [TBD] import はしないか
- [TBD] 端末間の引き継ぎはしないか

# イベント・会場

- [TBD] 以下の「イベント / 会場 / 演目」は上の Timeline / Slot / Participant と同一モデルか別案か
- [TBD] 1アプリ＝1タイムテーブルか、イベント＋会場（複数タイムライン）か
- [TBD] 会場が複数ある場合、タイムラインは会場ごと1本か

イベント
会場数
（各会場ごとに）
　会場名（複数ある場合）
　開始時間
　基本セッティング時間
　基本持ち時間

演目
　セッティング時間（基本と違う場合）
　持ち時間（基本と違う場合）
　出演順
　会場（複数ある場合）

その他項目（プリセット）
　客入れ時間
　休憩時間
　客出し時間


基本概念

項目マスタ
https://docs.google.com/spreadsheets/d/1QHhVQ7EJZNfDQ2P-Jauc1SaKmCB44iP33G8bqFcoJ0I/edit?gid=0#gid=0
