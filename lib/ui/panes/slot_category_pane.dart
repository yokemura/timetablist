import 'package:flutter/material.dart';

/// Top-left pane: slot category list and creation button.
/// Placeholder for now; real content arrives in the left-panes step.
class SlotCategoryPane extends StatelessWidget {
  const SlotCategoryPane({super.key});

  @override
  Widget build(BuildContext context) {
    return ColoredBox(color: Theme.of(context).colorScheme.surface);
  }
}
